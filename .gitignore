# Segredos — nunca commitar credenciais reais do Wazuh
scripts/.env
.env

# Python
scripts/__pycache__/
__pycache__/
*.pyc
scripts/.venv/
.venv/

# Estado local gerado em runtime (não é código-fonte)
scripts/system_alerts_history.json
scripts/*.log
scripts/*.log.err

# Estado local do Claude Code (não é conteúdo do projeto)
.claude/
